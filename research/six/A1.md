# A1 — the later west-category square cannot use the west cardinal side

## Statement

Work in the global normalization already established for the n=6 problem:

- the disk center is 0;
- the central square C uses the coordinate axes;
- c_x,c_y >= 0;
- the five exterior squares have cyclic primary order E,N,W,D,S;
- R^2 <= Q0 := 142559/50000.

Then D cannot use the west cardinal separator from C.

Since at most one exterior square may use a given central side, if D were
west-cardinal then W would necessarily use its own-primary separator.

This lemma is the A1 obligation in ROADMAP.md.

## Angle domain

Write the W and D primary directions as

    phi_W = pi + t,
    phi_D = pi + u,

with t <= u from the cyclic primary order.

If W uses its own-primary separator and t <= -2/3, the radial coordinate a_W
must satisfy

    a_W >= 1/2 + (2-rho0) cos(2/3) + (1/2) sin(2/3) > rho0,

where

    rho0 = sqrt(Q0-1/4)-1/2.

This contradicts the universal center bound a_W <= |p_W| <= rho0. Hence

    t > -2/3.

If D uses the west cardinal side, the sharp cap-depth formula gives

    |u| < 2/5.

Indeed the minimum west-side depth is 3/2-rho0, whereas at deviation 2/5

    B_Q0(2/5)
      = sqrt(Q0) - cos(2/5) - sin(2/5)
      < 3/2-rho0.

Therefore every hypothetical A1 configuration lies in the compact
two-angle triangle

    -2/3 < t <= u,
    -2/5 < u < 2/5.

The exact checker uses the closed rational superset

    -2/3 <= t <= 2/5,
    -2/5 <= u <= 2/5,
    t <= u.

The two scalar margins are respectively about

    0.0006493555
    0.0091181744.

## Three-separator stress

Let

    e(t)=(-cos t,-sin t),
    f(t)=( sin t,-cos t).

Assume D is west-cardinal and W is own-primary. We have two central
separations:

    (-1,0) dot (p_D-p_C) >= H_D,
    e(t) dot (p_W-p_C)  >= H_W,

where

    H_D = [1+cos u+|sin u|]/2,
    H_W = [1+cos t+|sin t|]/2.

By the separating-axis theorem, W and D are separated on one of

    +/- e(t), +/- f(t), +/- e(u), +/- f(u).

For a directed chosen W-to-D normal n,

    n dot (p_D-p_W) >= H_WD,

with

    H_WD = [1+cos(u-t)+sin(u-t)]/2,

because 0 <= u-t <= 16/15 < pi/2.

Multiply these three inequalities by the fixed rational weights

    A=3/10, B=9/20, M=1/4.

Forces on the three centers are

    G_C = -A(-1,0)-B e(t),
    G_W =  B e(t)-M n,
    G_D =  A(-1,0)+M n.

The global central bound gives

    G_C dot p_C
      <= (rho0-1)[max(G_Cx,0)+max(G_Cy,0)].

For either exterior square, with edge frame e,f and center p, containment in
B(0,sqrt(Q0)) gives the elementary support estimate

    G dot p
      <= sqrt(Q0)|G| - [|G dot e|+|G dot f|]/2.            (A1-support)

Proof: choose the square vertex v whose two frame signs agree with G. Then
G dot v=[|G dot e|+|G dot f|]/2, while |p+v|<=sqrt(Q0).

Thus every possible W-D separating direction would imply

    A H_D + B H_W + M H_WD
      <= U_C(G_C)+U_W(G_W)+U_D(G_D).                      (A1-ineq)

The exact two-angle replay proves the strict reverse inequality throughout
the triangle for all eight directed SAT axes.

## Exact replay

research/six/check_A1.py uses only:

- fractions.Fraction;
- integer square-root enclosures;
- alternating rational Taylor bounds for sine and cosine;
- deterministic bisection of the two rational angle intervals.

There are no center variables, numerical optimizers, floating-point proof
steps, or multidimensional packing boxes.

Replay statistics:

| W-D axis | direction | certified leaves | max depth |
|---|---:|---:|---:|
| W primary | + | 42 | 7 |
| W primary | - | 195 | 11 |
| W secondary | + | 1214 | 19 |
| W secondary | - | 270 | 13 |
| D primary | + | 84 | 8 |
| D primary | - | 92 | 9 |
| D secondary | + | 1511 | 15 |
| D secondary | - | 239 | 14 |

Total: 3647 exact rational leaves. On the current implementation the complete
replay takes about 22 seconds on the local CPU used during development.

Every directed separating-axis possibility contradicts (A1-ineq). Therefore

    D cannot be west-cardinal.

Hence, in the original c_x,c_y>=0 normalization used by the global sector and
pin lemmas,

    D uses its own-primary separator from C.

This closes roadmap item A1 without changing any of the conventions needed by
A2.
